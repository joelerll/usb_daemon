# instalar dependencias necesarias script de clonar las librerias que se necesitan, compilarlas

## dependencias , git, wget o curl


## dependencias de proyecto: jsmn, libmicrohttpd, mtab, libudev, libusb

## dependencias de cliente: python3, virtualenv, instalar los requirements .txt

## colocar el virtualenv en una carpeta llamada virtualenv/
